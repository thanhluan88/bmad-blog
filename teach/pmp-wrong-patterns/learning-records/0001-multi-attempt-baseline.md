# Multi-attempt baseline (luannt115 Full Bank)

Established that historical error signal is `attempts > 1` (248 IDs), not only `wrong > 0` (currently 0 after corrections). Dominant failure mode is Executing situational judgment on Stakeholders/Resources — not planning trivia. Hardest IDs include 828 (resilience), 981 (OPA update), 1047 (compliance no exception), 305 (transparency).

**Implications:** Teach trap recognition patterns before re-drilling individual IDs; keep People-domain mindsets ahead of formula drills.
